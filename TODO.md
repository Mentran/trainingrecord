# TODO — 运动训练记录

> 状态标记：`[ ]` 待开始 · `[~]` 进行中 · `[x]` 已完成 · `[-]` 已搁置

---

## 第一阶段 MVP

### Task 1 — 项目初始化
- [x] 用 Vite 创建 React + TypeScript 项目
- [x] 配置 Tailwind CSS
- [x] 配置基础路由（react-router-dom）
- [x] 确认开发服务器正常运行

### Task 2 — 数据层
- [x] 定义 `TrainingRecord` 数据结构（见 README）
- [x] 封装 localStorage 读写工具函数（getRecords / saveRecord / updateRecord / deleteRecord）
- [x] 封装教练列表的读写（从历史记录中自动提取）

### Task 3 — 录入页
- [x] 表单字段：日期（默认今天）、时长（分钟）、教练、训练内容、感悟
- [x] 表单校验：日期和训练内容为必填
- [x] 提交后保存到 localStorage
- [x] 支持编辑已有记录

### Task 4 — 列表视图
- [x] 按时间倒序展示所有记录卡片
- [x] 卡片显示：日期、教练、时长、内容摘要
- [x] 按教练筛选
- [x] 点击卡片查看完整记录详情
- [x] 详情页支持删除记录

### Task 5 — 日历视图
- [x] 月历组件，支持上下月切换
- [x] 有训练记录的日期高亮显示
- [x] 点击日期展示当天记录列表
- [x] 当天有多条记录时全部展示

### Task 6 — 数据备份
- [x] 设置页提供 JSON 导出
- [x] 设置页提供 JSON 导入
- [x] 导入前校验 JSON 结构，避免覆盖有效数据
- [x] 导入前提示用户确认覆盖或合并

### Task 7 — LLM 润色（可选增强）
- [ ] 设置页：填写 Claude API Key，存入 localStorage
- [ ] 详情页或编辑页提供「润色」按钮，手动触发 Claude API
- [ ] 对「训练内容」和「感悟」分别生成建议文案
- [ ] 展示原文和建议文案，允许用户一键应用或放弃
- [ ] 处理 API 调用失败的情况，不影响原文保存
- [ ] 未配置 API Key 时隐藏或禁用润色入口

### Task 8 — UI 打磨
- [x] 底部导航栏（录入 / 列表 / 日历 / 设置）
- [x] 移动端适配（主要使用场景是手机浏览器）
- [x] 空状态页面（无记录时的引导提示）
- [x] 加载状态和操作反馈（Toast 提示）

---

## 第二阶段 — AI 功能

### Task 9 — AI 润色
- [x] 设置页：填写 Claude API Key，存入 localStorage
- [x] 详情页加「润色」按钮，未配置 Key 时禁用并提示
- [x] 调用 Claude API 对训练内容和感悟分别生成润色建议
- [x] 展示原文 vs 润色版对比，用户选择应用或放弃
- [x] 应用后更新 `polishStatus` 为 `applied`，保留原文在 `contentOriginal`
- [x] API 调用失败时 Toast 提示，不影响原文

### Task 10 — AI 运动顾问
- [x] 新增「顾问」页面，底部导航加入口
- [x] 对话 UI：消息气泡，用户右侧 / AI 左侧，支持滚动
- [x] system prompt 约束：只回答运动相关问题，拒绝无关话题
- [x] 上下文注入：对话时自动附带最近 5 条训练记录摘要
- [x] 对话历史存 localStorage，下次打开可继续
- [x] 提供「清空对话」按钮
- [x] 未配置 API Key 时显示引导提示

---

## 第三阶段 — 多运动管理

### Task 11 — 数据层扩展（预埋，第二阶段末期做）
- [x] `TrainingRecord` 加 `sportId` 字段，现有记录迁移补默认值（`tennis`）
- [x] 新增 `Sport` 数据结构和 localStorage 读写
- [x] 默认创建「网球」运动项目，id 为 `tennis`
- [x] storage 工具函数支持按 `sportId` 过滤

### Task 12 — 运动项目管理 UI
- [x] 设置页新增「我的运动」入口
- [x] 运动列表页：展示已有运动，支持新增和删除
- [x] 新增运动：名称、emoji 图标、主题色选择
- [x] 删除运动时警告：该运动下的所有记录将一并删除

### Task 13 — 首页多运动切换
- [x] 首页顶部加运动切换 tab（只有 1 个运动时不显示）
- [x] 切换后统计数字、最近记录、主题色随之变化
- [x] 浮动录入按钮默认使用当前运动

### Task 14 — 全局主题色适配
- [x] 每个运动有独立主题色，替换全局的固定墨绿色
- [x] Header 渐变、主色 badge、热力图色阶跟随当前运动主题色

---

## 第四阶段 — 体验升级

### Task 15 — AI 配置统一化
- [x] 新增统一 AI 配置：`apiUrl` / `apiKey` / `model` 三个字段
- [x] 设置页 AI 区块展示三个输入项，支持独立编辑
- [x] `polishText` 和 `sendChatMessage` 改用统一配置，不再硬编码 URL 和 model
- [x] 兼容旧版只存了 `claude_api_key` 的用户数据

### Task 16 — 日历仪表盘升级
- [x] 日历页新增「年度」视图，GitHub 风格 52 周热力图
- [x] 月/年视图切换 tab
- [x] 年度视图显示全年训练分布，点击日期可查看记录
- [x] 年度统计：总次数、总时长、最长连续天数

### Task 17 — 全局视觉打磨
- [x] 底部导航：浮动胶囊样式，激活态用填充色块
- [x] TrainingCard：去掉左侧竖条，改为底部彩色细线 + 更精致的卡片设计
- [x] 首页 Header：更有层次感，数字更大，装饰圆背景，渐变延伸到 accentColor
- [x] 整体间距、字重、阴影系统重新梳理（card-shadow 升级，背景色更暖）
- [x] 空状态改为圆角图标块，更有设计感

- [x] 训练类型标签系统
- [x] 训练统计图表
- [x] PWA 支持（可安装 + 离线缓存 + 训练提醒）
- [ ] PDF 导出
- [ ] 云端同步

---

## 进度记录

| 日期 | 完成内容 |
|------|----------|
| 2026-05-21 | 完成需求讨论，输出 README 和 TODO，项目立项 |
| 2026-05-21 | 根据方案审查结果，收敛 LLM 复杂度，提前加入 JSON 备份任务 |
| 2026-05-21 | Task 1 完成：Vite + React + TS + Tailwind v4 + react-router-dom，四页骨架和底部导航就位，构建通过 |
| 2026-05-21 | Task 2 完成：TrainingRecord 类型定义，localStorage 读写封装，教练列表提取，JSON 导入导出 |
| 2026-05-21 | Task 3 完成：录入表单，日期/时长/教练/内容/感悟，必填校验，编辑模式，提交跳转列表页 |
| 2026-05-21 | Task 4/5/6/8 完成：列表页、详情页、日历页、设置页（JSON 备份）、Toast、空状态全部就位 |
| 2026-05-21 | UI 全面重设计：深色 Header、仪表盘首页、热力图日历、彩色教练竖条、SVG 导航图标 |
| 2026-05-21 | 需求扩展：规划第二阶段（AI 润色 + AI 运动顾问）和第三阶段（多运动管理），更新文档 |
| 2026-05-21 | Task 9 完成：Claude API Key 配置（设置页），详情页 AI 润色按钮，原文 vs 润色对比，应用/放弃选择 |
| 2026-05-21 | Task 10 完成：AI 运动顾问对话页，训练记录上下文注入，对话历史持久化，建议问题引导 |
| 2026-05-21 | 自审优化：修复 textarea 兼容性、各页数据刷新、JSON 解析容错、对话页高度布局 |
| 2026-05-21 | Task 15-17 完成：AI 配置统一化（url/apiKey/model）、日历年度热力图仪表盘、全局视觉打磨（浮动胶囊导航、卡片底部彩线、首页装饰圆） |
| 2026-05-21 | Task 18-20 完成：日历统计 tab（每周次数/每月时长/教练分布 SVG 图表）、训练标签系统（预设+自定义，列表页标签筛选）、PWA 支持（manifest + service worker + 训练提醒） |
